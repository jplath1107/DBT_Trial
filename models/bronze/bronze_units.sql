{{
    config(
        materialized='view'
    )
}}

select
date,
    ununit as tractor_code,
    unown as owner_id,
    "UNCO#" as company_number,
    "UNDV#" as division_number,
    "UNTM#" as terminal_number,
    unyear as year_made,
    unmake as make,
    unser as serial_number,
    unplat as base_plate,
    unplst as base_state,
    untnt as new_york_tnt,
    unhut as ohio_hut,
    unterm as termination_date,
    uncost as cost,
    unins as insurance_amount,
    unpayt as pay_type,
    unperc as pay_percent,
    unlrat as loaded_rate,
    unerat as empty_rate,
    unhirw as hire_date,
    undr1 as driver_one,
    undr2 as driver_two,
    untrl1 as trailer_1,
    unpdr1 as previous_driver_1,
    unpdr2 as previous_driver_2,
    unptr1 as previous_trailer_1,
    unstat as status,
    "UNORD#" as order_assigned,
    undisp as dispatch_number,
    unprvo as previous_order,
    unpdsp as previous_dispatch_number,
    undcty as destination_city,
    undst as destination_state,
    undzn as destination_zone,
    undrg as destination_region,
    undbea as destination_bea_code,
    undgu as destination_gu_code,
    undsnm as destination_short_name,
    unewgt as empty_weight,
    ungrwt as gross_weight,
    unetad as eta_date,
    unetat as eta_time,
    unecdt as eta_change_date,
    uncntd as contact_date,
    uncntt as contact_time,
    uncnti as contact_initial,
    unccty as contact_city,
    uncst as contact_state,
    uncsnm as contact_short_name,
    unmess as message_flag,
    undel as delivery_code,
    untype as unit_type,
    unaxle as number_of_axles,
    unflet as fleet_number,
    undhor as deadhead_origin,
    unpreo as preplan_order,
    uncmtm as preplan_commitment,
    unfuel as fuel_type,
    unavas as avas_code,
    undara as destination_area,
    unlhub as last_hub,
    unlprd as last_purchase_date,
    unlprt as last_purchase_time,
    unlgal as last_gallons,
    unlmpg as last_mpg,
    un1gal as first_gallons,
    un1mpg as first_mpg,
    un1hub as first_hub,
    un1prd as first_purchase_date,
    un1prt as first_purchase_time,
    unpgal as previous_gallons,
    unpmpg as previous_mpg,
    unphub as previous_hub,
    unpprd as previous_purchase_date,
    unpprt as previous_purchase_time,
    uncosc as cost_center,
    unedis as latitude,
    unndis as longitude,
    unmult as multiple_trailers,
    unlen as length,
    unaxs as axle_spread,
    unflic as fleet_license,
    uneng as engine_size,
    unmisc as miscellaneous_remarks,
    unfcap as fuel_capacity,
    unpurd as purchase_date,
    unlcod as two_character_code,
    unwrkc as canadian_workers_compensation_code,
    unlsts as last_satellite_location,
    unssnm as satellite_location_short_name,
    unnxtl as next_location,
    unnsnm as next_stop_short_name,
    unpta as projected_time_available,
    unpda as projected_date_available,
    unpcdt as projected_time_available_change_date,
    unsupr as supervisor_code,
    unfmgr as fleet_manager,
    unserv as service_fail_flag,
    uncstf as customer_site_flag,
    uncred as creation_date,
    uncret as creation_time,
    uncrei as creation_initial,
    unupdd as last_update_date,
    unupdt as last_update_time,
    unupdi as last_update_initial,
    unownt as owner_operator_owns_trailer,
    unubal as unit_advance_balance,
    uncbal as company_advance_balance,
    unctyt as city_truck_flag,
    unmodl as unit_model,
    ungate as gate_check_in_location,
    undcst as dedicated_customer,
    unedfl as dedication_flag,
    untyp2 as unit_type_new,
    unefm as exclude_from_model_flag,
    unavld as available_for_limited_dispatch,
    unhdte as temporary_hold_date,
    unusr1 as user_defined_date,
    unusr2 as user_defined_field_2,
    unusr3 as user_defined_field_3,
    unusr4 as user_defined_field_4,
    unfil as filler,
    u1_supid as subsidiary_legal_entity_id,
    u1_dburi as database_record_id,
    u1_dbpri as database_parent_record_id,
    u1_wtims as write_timestamp,
    u1_wuser as writer_user_id,
    u1_utims as update_timestamp,
    u1_uuser as update_user_id,
    "U1_PORD#" as order_prefix,
    "U1_SORD#" as order_suffix,
    u1_revt1 as revenue_type_1,
    u1_revt2 as revenue_type_2,
    u1_revt3 as revenue_type_3,
    u1_revt4 as revenue_type_4,
    unspeq as special_equipment_code,
    unrfid as rfid_tag_flag
from {{ source('iesfilec', 'units') }}
join {{ source('dbo', 'dim_date') }} on uncred = juldate
